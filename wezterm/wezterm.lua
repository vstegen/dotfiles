local wezterm = require("wezterm")

local config = {
	color_scheme = "Catppuccin Mocha",
	font = wezterm.font({
		family = "MonoLisa Custom",
		weight = "Bold",
	}),
	font_size = 14.0,
}

return config
